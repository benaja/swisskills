<?php

/**
 * First syntax error
 *
 * @todo change the IF LINE only, while retaining the logical implementation
 */

define("KEY", "ecbc2ae0794d39394ebe");

if (KEY) {
    
    //
    // here some work is going on when the key constant is set
    // ...

    $params['solvedErrors']['the first syntax error'] = true;
}
